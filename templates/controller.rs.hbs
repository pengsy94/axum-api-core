use axum::{Json, extract::Path};
use serde::{Deserialize, Serialize};
use serde_json::{Value, json};

// ========================================
// {{Name}} Controller
// 自动生成，按需修改
// ========================================

// ---- 请求/响应类型 ----

/// 列表查询参数
#[derive(Debug, Deserialize)]
pub struct IndexQuery {
    pub page: Option<u32>,
    pub page_size: Option<u32>,
}

/// 创建请求
#[derive(Debug, Deserialize)]
pub struct CreateRequest {
    pub name: String,
}

/// 更新请求
#[derive(Debug, Deserialize)]
pub struct UpdateRequest {
    pub name: String,
}

/// 响应
#[derive(Debug, Serialize)]
pub struct {{Name}}Response {
    pub id: i32,
    pub name: String,
}

// ---- Handler ----

/// 列表
pub async fn index() -> Json<Value> {
    // TODO: 从数据库查询
    Json(json!({ "data": [], "total": 0 }))
}

/// 详情
pub async fn show(Path(id): Path<i32>) -> Json<Value> {
    // TODO: 从数据库查询单条
    Json(json!({ "id": id, "name": "" }))
}

/// 新增
pub async fn create() -> Json<Value> {
    // TODO: 写入数据库
    Json(json!({ "id": 0 }))
}

/// 更新
pub async fn update(Path(_id): Path<i32>) -> Json<Value> {
    // TODO: 更新数据库
    Json(json!({ "success": true }))
}

/// 删除
pub async fn delete(Path(_id): Path<i32>) -> Json<Value> {
    // TODO: 删除数据库记录
    Json(json!({ "success": true }))
}
